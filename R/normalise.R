#' Wrapper function to scale each feature vector into a user-specified range for visualisation and modelling
#' 
#' @inheritParams normaliseR::normalise
#' @return either an object of class \code{data.frame} or a \code{numeric} vector
#' @author Trent Henderson
#' @export
#'

normalise <- normaliseR::normalise


#' Wrapper function to scale each feature vector into a user-specified range for visualisation and modelling
#' 
#' @inheritParams normaliseR::normalize
#' @return either an object of class \code{data.frame} or a \code{numeric} vector
#' @author Trent Henderson
#' @export
#'

normalize <- normaliseR::normalize
